#include "PathFinding.h"

#include "Grid.h"

void PathFinding::FindNeighbours(Direction direction, Grid::Tile currentTile)
{
    // Find the neighbours of the current node
    // If the node is at the edge of the grid, then the neighbour is the opposite direction
    // If the node is not at the edge of the grid, then the neighbour is the opposite direction and the direction
    // If the node is at the corner of the grid, then the neighbour is the opposite direction and the direction
    // If the node is not at the corner of the grid, then the neighbour is the opposite direction and the direction
}
